# ${{ values.name }} (.NET Web API)

This project is generated by the Helios Backstage .NET template.

Pinned versions:

- .NET SDK: 10.0.300 (feature-band for .NET 10.0.8 runtime)
- .NET runtime: 10.0.8
{% if values.hasDatabase -%}
- PostgreSQL: 18.4 (via Docker Compose)
{%- endif %}

## Local Development

1. Start application:

```bash
docker compose up --build
```

2. Open Swagger:

http://localhost:${{ values.port }}/swagger
{% if values.hasDatabase %}
## Environment Variables

- DB_HOST
- DB_USER
- DB_PASSWORD
- DB_NAME

The API builds `ConnectionStrings:DefaultConnection` at startup from these values.
{% endif %}
